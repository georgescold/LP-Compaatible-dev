/**
 * OBSOLETE — Tailwind CSS v4 n'utilise plus ce fichier.
 * Toute la configuration est dans src/style.css via @theme.
 * Ce fichier est conservé uniquement pour compatibilité avec certains IDE.
 */
export default {
  content: [
    "./index.html",
    "./src/**/*.{vue,js,ts,jsx,tsx}",
  ],
}
